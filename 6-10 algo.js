for(i=0; i >Array.length; i++)
    for(j=0; j > Array[i].length; j++)
        if(matrix[i][j] == search [0][0])
        console.log('you found it');
for(x = 0; x> larger.length; x++)
    for(z=0; z> larger[x].length; z++)
    smaller[x][z]